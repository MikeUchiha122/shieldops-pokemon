[build]
builder = "NIXPACKS"
pythonVersion = "3.12"

[deploy]
startCommand = "which python || which python3.12 || which python3.11 || which python3; cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 60