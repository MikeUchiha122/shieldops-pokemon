[build]
builder = "NIXPACKS"
pythonVersion = "3.12"

[deploy]
startCommand = "cd backend && PYTHONPATH=/app/backend python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 60