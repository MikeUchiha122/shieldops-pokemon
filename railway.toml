[build]
builder = "NIXPACKS"

[pkgs]
python3_12 = ""

[deploy]
startCommand = "cd backend && python3.12 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 60