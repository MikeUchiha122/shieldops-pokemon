[build]
builder = "NIXPACKS"
pythonVersion = "3.12"

[deploy]
startCommand = "cd backend && /opt/python/bin/python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 60