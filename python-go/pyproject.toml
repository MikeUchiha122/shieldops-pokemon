[tool.pytest.ini_options]
testpaths = ["tests"]
addopts   = "--cov=core --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["core"]
omit   = ["*/tests/*","*/__init__.py"]

[tool.bandit]
exclude_dirs = ["tests"]
skips        = ["B101"]
